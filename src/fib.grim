@var n:int = 40;

@fn fib(n:int) <int> {
    @if (int/lt n 2) {
        @ret n;
    }
    @ret (fib (int/add (int/sub n 1) (int/sub n 2)));
}

@fn main() {
    @var res:int = (fib n);
    (println res)
    @ret;
}