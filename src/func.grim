@fn main() {
    @if (int/lt tmp 2) {
        @var res:int = (int/add 20 48);
        @var test:int = 0;
        @if (int/eq res 68) {
            @set tmp = res;
            @var test:int = 0;
        }
    }
    @var res:int = 0;
}
